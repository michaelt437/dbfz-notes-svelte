export type Combo = {
  inputs: string;
  title: string;
  video: string;
  tags: string[];
  tod: boolean;
  date: string;
  damage?: number;
  tips: string[];
};

export type Character = {
  name: string;
  id: string;
};
