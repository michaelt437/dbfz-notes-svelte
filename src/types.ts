export type Combo = {
  inputs: string;
  position: string;
  title: string;
  video: string;
  tod: boolean;
  damage?: number;
  tips?: string[];
}

export type Character = {
  name: string;
  id: string;
}